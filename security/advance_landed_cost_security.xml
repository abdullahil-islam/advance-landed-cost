<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <!--
            Multi-company record rules are added here as models are created
            in Phase 2 onward. Each rule uses the domain:
            ['|', ('company_id', '=', False), ('company_id', 'in', company_ids)]
            so users see their own company's records plus any global records
            (company_id = False).
        -->
    </data>
</odoo>
